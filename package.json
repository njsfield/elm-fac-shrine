{
  "name": "elm-fac-shrine",
  "version": "0.0.1",
  "description": "A shrine to Founders & Coders",
  "dependencies": {
    "elm": "^0.18.0"
  },
  "scripts": {
    "elm-install": "node_modules/.bin/elm-package install -y",
    "postinstall": "npm run elm-install",
    "build": "elm-live Main.elm --open --pushstate --output=elm.js",
    "make": "node_modules/.bin/"
  }
}
